# Scan secrets in repository to prevent fraudulent use

  * Scans contents for sensitive data, such as API keys, passwords, tokens, and other secrets.
  * When secrets are found, report users that is correctly configured in .gitignore file.